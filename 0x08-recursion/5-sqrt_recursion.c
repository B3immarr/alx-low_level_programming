#include "main.h"

/**
 * _sqrt_recursion - returns natural square root of a number
 * @n: number which sqrt should be found
 * Return: natural sqrt of n
 * if n has no natural sqrt function should return -1
 */
int _sqrt_recursion(int n)
{
if (n < 0)
	return (-1);
return (number(1, n));
}
